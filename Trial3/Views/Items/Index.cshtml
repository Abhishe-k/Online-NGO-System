@model IEnumerable<Trial3.Models.Items>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ItemsName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ItemsName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ItemsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ItemsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ItemsId })
        </td>
    </tr>
}

</table>
